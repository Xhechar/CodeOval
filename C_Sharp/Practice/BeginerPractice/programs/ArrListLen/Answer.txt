public class ArrayListLength
{
    public static int Solution(int[] A)
    {
        int length = 0;
        int index = 0;

        while (index != -1)
        {
            index = A[index];
            length++;
        }

        return length;
    }
}